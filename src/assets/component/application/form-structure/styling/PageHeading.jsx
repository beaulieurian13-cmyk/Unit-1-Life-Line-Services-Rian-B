const PageHeading = ({ title }) => {
  return (
    <>
      // TODO: add h2 for title of page and h3 for tile of section as option
      <h2 className="h2">{title}</h2>
    </>
  );
};

export default PageHeading;
